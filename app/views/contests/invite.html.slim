- set_meta_tags title: '家族や友人を招待する'
- set_meta_tags description: 'コンテストに家族や友人を招待するためのページです。'

.container.mx-auto.max-w-xl.p-6
  h1.text-2xl.font-bold.mb-4 家族や友人を招待する

  p.text-stone-600
    | 以下のURLからログインすることで、このコンテストに参加することができます。
    br
    | URLをコピーして家族や友人をコンテストに招待しましょう！

  .mt-6
    input#invite-link(
      type="text"
      value=@invite_url
      readonly=true
      class="w-full p-2 text-lg border border-stone-300 rounded-lg shadow-sm"
    )

      .mt-6.text-center
        = button_tag '招待用URLをコピー',
          data: { controller: 'clipboard',
                  action: 'clipboard#copy',
                  clipboard_url_value: @invite_url },
          class: 'bg-cyan-500 text-white font-bold py-3 px-4 \
                  rounded-full hover:bg-cyan-600 shadow-md'

  p.mt-6.text-center
    = link_to 'キャンセル', edit_contest_path(@contest),
      class: 'text-stone-700 hover:underline'
